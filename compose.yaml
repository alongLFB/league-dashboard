name: league-dashboard

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: league-dashboard-app
    restart: unless-stopped
    ports:
      - "127.0.0.1:${APP_PORT:-3021}:3021"
    env_file:
      - .env
